// COLOUR PICKER ENTRY — include only on pages with the colour picker via
// Page Settings > Custom Code.
import { onReady } from '../utils/ready.js';

onReady(() => {
  const picker = document.querySelector('[data-colour-picker]');
  if (!picker) return;

  // --- paste your texture fade-in + base64 placeholder logic here ---
});
