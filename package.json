{
  "name": "webflow-scripts",
  "version": "1.2.4",
  "description": "Custom JavaScript bundles for the Nu-Wall Webflow site, built with Vite and served via jsDelivr.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "node scripts/build.js && echo Use ENTRY=global vite build --watch for single-entry watch",
    "build:css": "node scripts/build-css.js",
    "build": "node scripts/build.js && npm run build:css",
    "clean": "rimraf dist",
    "release": "npm run build && node scripts/release.js"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "sass": "^1.79.0",
    "vite": "^6.0.7"
  },
  "dependencies": {
    "swiper": "^12.2.0"
  }
}
